import 'package:flutter/material.dart';

class UserProfileMediaTab extends StatelessWidget {
  const UserProfileMediaTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Text("media"),
    );
  }
}
